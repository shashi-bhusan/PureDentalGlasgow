# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*.swo

# Env / secrets (never commit Hostinger or SMTP passwords)
.env
.env.local
.env.production
!.env.example
*.pem

# PHP
vendor/
composer.phar

# Logs & local server
*.log
php_errors.log

# Hostinger full dump name (if you keep a second copy outside public/)
public_html/

# Scratch mirror output (canonical tracked site is public/)
site-mirror/

# Local deploy scripts with real credentials
scripts/deploy-hostinger.sh
scripts/deploy-staging.sh

dist/
public-staging-contents.zip
public-staging-upload.zip
*.zip
*.tar.gz

# Written only by CI before staging FTP upload (do not commit)
public/STAGING_DEPLOY_CHECK.txt
